{
	"extends": "./tsconfig.base.json",
	"compilerOptions": {
		"outDir": "${configDir}/dist",
		"rootDir": "${configDir}/src",
		"composite": true,
		"tsBuildInfoFile": "${configDir}/node_modules/.tmp/tsconfig.src.tsbuildinfo"
	},
	"include": ["${configDir}/src"]
}
